# Use what you’ve learned in the previous slide to create an operator and a sensor
# Create an operator that bakes multiple pizzas and returns a list of IDs
# Create a sensor that waits for a pizza to be baked (assume baking a pizza takes 5 seconds) 
# Create a new DAG with these 2 operators operators
# Play around with the sensor parameters (poke_interval, mode, timeout)
# * Extra points: create a macro that checks if a pizza is baked (assume there is a 1 in 2 chance a random pizza is baked)
